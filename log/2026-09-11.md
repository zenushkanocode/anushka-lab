# 2026-09-11

**What shipped:** Friday ship. Added today's log plus `scratch/gap.sh` — counts consecutive Asia/Calcutta days *without* a log ending today (0 if today is filled). Companion to `streak.sh`.

**Why it matters:** Streak tells you the win streak; gap tells you how deep the hole is when a day slips — same one-liner habit, opposite question.
