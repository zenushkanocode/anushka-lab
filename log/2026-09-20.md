# 2026-09-20

**What shipped:** Sunday ship. Added today's log plus `scratch/month-fill.sh` — Asia/Calcutta full-month view (every calendar day, not just weekdays): logged through today, days still ahead, and whether a perfect month is still reachable.

**Why it matters:** This habit runs Sat/Sun too. Weekday-only helpers miss weekend gaps; this tells you in one glance if September can still finish with every day logged.
