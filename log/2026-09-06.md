# 2026-09-06

**What shipped:** Sunday ship. Added today's log plus `scratch/streak.sh` — walks backward from today (Asia/Calcutta) counting consecutive days that already have a `log/YYYY-MM-DD.md`.

**Why it matters:** The graph only shows presence; this prints the current streak length so gaps are obvious before the square goes blank.
