# 2026-09-05

**What shipped:** Weekend green-square day. Added today's log plus `scratch/missing-logs.sh` — walks back N days (default 7) and lists any Asia/Calcutta dates missing a `log/YYYY-MM-DD.md`.

**Why it matters:** Streak breaks are silent until the graph goes blank. This makes gaps loud so the daily ship can catch up instead of guessing.