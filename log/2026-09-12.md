# 2026-09-12

**What shipped:** Saturday ship. Added today's log plus `scratch/longest-streak.sh` — walks the full `log/` history and reports the longest consecutive Asia/Calcutta day run (not just the streak ending today).

**Why it matters:** Current streak is a live pulse; longest streak is the personal best. Same habit, better scoreboard.
